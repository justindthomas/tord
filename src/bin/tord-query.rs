//! tord-query — CLI for the `/run/tord.sock` control socket.
//!
//! Mirrors `dnsd-query`. Installed on the appliance as
//! `imp-tord-query`. See DESIGN.md §10.

fn main() {
    eprintln!("tord-query: not yet implemented — see DESIGN.md §12 (phase 5)");
    std::process::exit(1);
}
